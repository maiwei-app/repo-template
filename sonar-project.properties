# organization and projectKey are set by ci.yml's sonar-scan.yml call
# (derived from github.repository), not read from here — CLI args override
# this file. Kept minimal so there's nothing stale to drift from CI.
sonar.sources=.
